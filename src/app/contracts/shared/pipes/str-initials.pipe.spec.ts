import { StrInitialsPipe } from './str-initials.pipe';

describe('StrInitialsPipe', () => {
  it('create an instance', () => {
    const pipe = new StrInitialsPipe();
    expect(pipe).toBeTruthy();
  });
});
